function Lang8Features__IsPatternExpression() {
  var x, t, asdf, z, y;
  x = 90;
  t = x == 900;
  asdf = (z = Type__AsType(Int32, x)) != null;
  if (x == 900);
  else if ((y = Type__AsType(Int32, x)) != null);
}
